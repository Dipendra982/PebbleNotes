# PebbleNotes — Environment variables for Vercel deployment

# ===== Backend (Express) =====
# Comma-separated list of allowed frontend origins for CORS
FRONTEND_URLS=https://pebblenotes.vercel.app
# Fallback single origin (used in some email links)
FRONTEND_URL=https://pebblenotes.vercel.app
# Public backend base URL (used in verification email links)
BACKEND_URL=https://pebblenotes-backend.vercel.app
# JWT settings
JWT_SECRET=replace_with_long_random_string
JWT_EXPIRES_IN=7d
# Optional dev-only admin bypass header password
ADMIN_DEV_PASSWORD=

# ===== SMTP (optional but recommended for production email) =====
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="PebbleNotes <no-reply@yourdomain.com>"

# ===== PostgreSQL database =====
DB_HOST=
DB_PORT=5432
DB_NAME=pebblenotes
DB_USER=
DB_PASSWORD=
DB_POOL_MAX=10
DB_POOL_MIN=2

# ===== Frontend (Vite build) =====
# Gemini API key used by frontend build via vite.config.js
GEMINI_API_KEY=
