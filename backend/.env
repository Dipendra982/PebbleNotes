# PebbleNotes Backend Environment Configuration

# Server
PORT=4000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pebblenotes
DB_USER=postgres
DB_PASSWORD=root
DATABASE_URL=postgresql://postgres:root@localhost:5432/pebblenotes

# JWT Secrets
JWT_SECRET=pebblenotes-jwt-secret-key-2024
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=pebblenotes-refresh-secret-key-2024
JWT_REFRESH_EXPIRES_IN=30d

# Frontend URL (for CORS)
# Use LAN IP so mobile devices can reach the dev server
FRONTEND_URL=http://192.168.1.66:3000
FRONTEND_URLS=http://localhost:5173,http://localhost:3000,http://192.168.1.66:3000

# Dev admin bypass (for college project simplicity)
ADMIN_DEV_PASSWORD=root

# Backend URL (for email links) - use LAN IP so mobile can access
BACKEND_URL=http://192.168.1.66:4000

# SMTP configuration for Nodemailer (REAL Gmail SMTP)
# Use Gmail App Password (no spaces) and your Gmail address
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=sahdipendra400@gmail.com
SMTP_PASS=hmxnoovatzrguwgt
SMTP_FROM="PebbleNotes <sahdipendra400@gmail.com>"
EMAIL_PROVIDER=gmail
