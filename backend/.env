# PebbleNotes Backend Environment Configuration

# Server
PORT=4000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5433
DB_NAME=pebblenotes
DB_USER=postgres
DB_PASSWORD=roots
DATABASE_URL=postgresql://postgres:roots@localhost:5433/pebblenotes

# JWT Secrets
JWT_SECRET=pebblenotes-jwt-secret-key-2024
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=pebblenotes-refresh-secret-key-2024
JWT_REFRESH_EXPIRES_IN=30d

# Frontend URL (for CORS)
# Use localhost for local development
FRONTEND_URL=http://localhost:3000
FRONTEND_URLS=http://localhost:5173,http://localhost:3000,http://localhost:3000,http://192.168.1.64:3000

# Dev admin bypass (for college project simplicity)
ADMIN_DEV_PASSWORD=roots

# Backend URL (for email links) - use localhost for local development
BACKEND_URL=http://localhost:4000

# SMTP configuration for Nodemailer (REAL Gmail SMTP)
# Use Gmail App Password (no spaces) and your Gmail address
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=sahsurajj07@gmail.com
SMTP_PASS=foszklrjuyudovba
SMTP_FROM="PebbleNotes <sahsurajj07@gmail.com>"
